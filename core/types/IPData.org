* IPData
This is the fresh-collected data. So it lacks the duration and timestamp
fields. 

#+begin_src cpp :tangle IPData.cppm
export module IPData;
import <string>;

import net_integer;
import Statistics;

using lpprogramming::types::net_integer::net_u128;

namespace bandwidthd {
  export class IPData {
  public:
    std::string label{};
    net_u128 ip;
    Statistics Sent;
    Statistics Received;
  };
}

#+end_src
